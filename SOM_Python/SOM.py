import node

class SOM(object):

	def __init__(self, xClient, yClient, cellsUp, cellsAcross, numIterations)
		self.xClient = xClient
		self.yClient = yClient
		self.cellsUp = cellsUp
		self.cellsAcross = cellsAcross 
		self.numIterations = numIterations 
		self.cellWidth = xClient/cellsAcross
		self.cellHeight = yClient/cellsUp
		self.SOM = [0]		
		self.mapRadius = maX(Constant.

	def CreateNodes(self)
		row = 0 
		n = cellsUp
		col = 0
		m = cellsAcross
		for row in range (0, n)
			for col in range (0, m)
				SOM.append(Node(col * self.cellWidth, (col + 1) * cellWidth, row * cellHeight, (row + 1) * cellHeight, constSizeOfInputVector))

		

		#give a vector of input vectors this method choses one at random and runs the network through one training epoch 
		def Epoch(self,data):
		if (len(data[0]) != Constants.constSizeOfInputVector)
			return false
 		
		if (self.numIterations > 0)
		

 
		
	
